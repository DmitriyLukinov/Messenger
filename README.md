To successfully launch the project, the framework Express.js and database MySQL (mysql2) must be installed. 
A table "users_messages" with four columns -id, user, message, time- must be created in the database.
# messanger
