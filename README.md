To successfully launch the project, the framework Express.js and database MySQL must be installed. 
A table "users_messages" with four columns must be created in the database: id, user, message, time.
# messanger
